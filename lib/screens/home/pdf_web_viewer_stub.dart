import 'package:flutter/widgets.dart';

const bool isWebPdfViewerSupported = false;

Widget buildWebPdfViewer(String pdfUrl) {
  throw UnsupportedError('Web PDF viewer is only available on Flutter web.');
}
